# name: ci

# on: [push, pull_request]

# jobs:
#   docker:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: justintime50/composite-actions/bootstrap-php@v1
#         with:
#           php-version: '8.5'
#       - name: Run setup Docker container
#         run: |
#           cd src
#           cp .env-example .env
#           just setup
#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: justintime50/composite-actions/bootstrap-php@v1
#         with:
#           php-version: '8.5'
#       - name: Lint project
#         run: |
#           cd src
#           just install lint
#   unit-test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: justintime50/composite-actions/bootstrap-php@v1
#         with:
#           php-version: '8.5'
#       - name: Install Dependencies
#         run: |
#           cd src
#           cp .env-example .env
#           just setup
#       - name: Run tests
#         run: |
#           cd src
#           just coverage
#   browser-test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: justintime50/composite-actions/bootstrap-php@v1
#         with:
#           php-version: '8.5'
#       - name: Setup Dependencies
#         run: |
#           cd src
#           cp .env-example .env
#           just setup
#           just build
#       - name: Run tests
#         run: |
#           cd src
#           php artisan serve --port=8686 > /dev/null 2>&1 &
#           sleep 5
#           just dusk
